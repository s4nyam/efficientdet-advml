# Runtime dependencies for the deepseanet package.
# Install the package instead where possible:  pip install -e '.[torch]'
numpy>=1.21
pillow>=9.0
pyyaml>=6.0

# Uncomment for the model, attack and CAM modules (anything importing torch):
# torch>=2.0
# torchvision>=0.15

# Uncomment for overlay_cam and plotting:
# opencv-python>=4.6
# matplotlib>=3.5
